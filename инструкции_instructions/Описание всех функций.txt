1)CreatePool -  //Функция для создания пула принимает имя пула желательно чтобы было  в формате "Gerda-Krendel" , адрес первого токена и адрес второго токена
    //какие хотите чтобы менялись, цена первого и второго токена в формате чтобы подходила для деления на 10, адрес лп токена профи, стартовое соотношение в eth
    //На выходе создается новый пул

2)giveStartTokensToUsers (Factory.sol) -   //Функция позволяет распределить какое то количество стартовых токенов нашим пользователям,  от Owner, принимает количество токенов, вызывает методы из интерфейса Token 
3)getAllPoolsAddress -     //Функция геттер для получения массивов адресов пулов
4)transferTokens -  //Самописная функция для перевода токенов, принимает адрес от кого хотите передать, кому хотите, количество
    //Возвращает в случее успеха true
5)mint -  //Самописная функция для выпуска токенов принимает адрес кому на баланс токены попадут, и количество токенов
6) giveStartTokensToUsers (Token.sol) - //Функция для выдачи стартовых токенов 3 пользователям Tom,Rick, Ben .принимает количество токенов
7)decimals - //Функция для вывода количество десятичных знаков у токена по умолчанию 12, возвращает количество десятичных знаков
8)getFirstTokenPriceEth - //Функция для вывода стоимости первого токена в eth возращает стоимость первого токена
9) getSecondTokenPriceEth - //Функция для вывода стоимости второго токена в eth возращает стоимость второго токена
10)addFirstTokenLiquidity -   //Функция позволяет добавить ликвидность в пул на первый токен, принимает количество первого токена  которое заберется и попадет в пул
11)addSecondTokenLiquidity -  //Функция позволяет добавить ликвидность в пул на второй токен, принимает количество второго токена которое заберется и попадет в пул
12)tradeFirstToSecond -    //Функция для обмена первого токена на второй, принимает количество первого токена  возвращает количество второго токена которое нужно получить
13)tradeSecondToFirst -  //Функция для обмена второго токена на первый, принимает количество второго токена возвращает количество первого токена которое нужно получить
14)getFirstTokenReserve -  //Функция для получения резерва первого токена возвращает количество первого токена в пуле 
14)getSecondTokenReserve -//Функция для получения резерва второго токена возвращает количество второго токена в пуле 
15) showNonStackedTokens -      //Функция для показа баланса пользователя который не лежит на стейкинге, возвращает число токенов
16) putLPProfi -   //Функция для того чтобы положить лп токены на стейкинг контракт, принимает количество токенов
17) takeReward -   //Функция для забирания награды со стейкинга
18)getValueAllStackingLP -     //Функция для получения количество лп токенов на стейкинге
19) returnMyStack - //Функция для того чтобы забрать свои лп токены со стейкинга
20)changeGerdaToRTK -     //Функция для обмена токена Gerda на rtk, принимает количество gerda которое хотите отдать
21) changeRTKToGerda -  //Функция для обмена токена RTK на GERDA, принимает количество RTK которое хотите отдать